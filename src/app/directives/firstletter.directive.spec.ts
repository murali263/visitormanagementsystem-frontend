import { FirstletterDirective } from './firstletter.directive';

describe('FirstletterDirective', () => {
  it('should create an instance', () => {
    const directive = new FirstletterDirective();
    expect(directive).toBeTruthy();
  });
});
